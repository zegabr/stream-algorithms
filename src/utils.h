#ifndef UTILS_TAA
#define UTILS_TAA 1
#include <vector>

class Utils{
 // TODO: aqui vao ficar metodos tipo: ler de csv, criar funcoes hash, pegar parametros (ou a gt pode separar em 3 tbm)
    private:

    public:
        std::vector<int> getNewHashFunction(int P){
            // TODO: generate random a and b mod P
            return {1,2};
        }
};

#endif